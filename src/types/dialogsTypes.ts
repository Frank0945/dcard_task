export interface IAlert {
    title: string;
    message: string;
    confirm: string;
}

export interface IConfirm {
    title: string;
    message: string;
    confirm: string;
    cancel: string;
}